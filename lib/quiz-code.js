const start = newDate();
const ellapsed = newDate() - start;


setTimeout(function () {
    if ellapsed
    result = console.log('5s later...');
}, 7000);
if (err) return (err);
callback(null, data);

module.exports = setTimeout;
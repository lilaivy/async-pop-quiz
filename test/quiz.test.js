const assert = require('assert');
const setTimeout = require('../lib/quiz-code');

const start = newDate();
const ellapsed = newDate() - start;


describe(takeFive(), => {

    it('will call the callback after 5 seconds', done => {
     //test that the callback runs after a 5 second time out
}).timeout(7000);
done();

});

